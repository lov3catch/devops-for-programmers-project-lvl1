name: ci

on: push

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Tests
        run: make test
      - name: Build new Image
        run: |
          make build
          make push